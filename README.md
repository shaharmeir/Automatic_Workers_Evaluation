# Automatic_Workers_Evaluation
Code for the research about evaluating workers when they textually answer mutually exclusive set of questions independently, by using a language model as a ground truth
